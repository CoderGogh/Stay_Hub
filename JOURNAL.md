# JOURNAL

7일 제출 기한 중, 요구사항 분석부터 필수 구현(①~⑥)까지의 진행 과정을 기록합니다.

## Day 1 — 요구사항 분석 및 필수 구현

### 수행 내용
- 과제 안내 문서를 읽고 요구사항 정리 (문서 원본은 저장소에 커밋하지 않음)
- 브랜치 전략(main/develop/feat/docs) 및 커밋·PR·이슈 컨벤션 수립
- 표준 숙박 상품 모델(Accommodation, RoomType) 설계 및 매핑 저장 구조 구현
- Supplier 연동 어댑터(WebClient) 구현 — 공통 포트 + 공급사별(A/B) 어댑터
- Mock Supplier 구성 (mock 프로필, 포트 9090) — 정상/장애/무응답 모드 토글
- 카탈로그 동기화 서비스 (기동 시 1회 매핑 upsert)
- 통합 검색 API (병렬 조회 + 정규화 병합 + 부분 실패 허용)
- README.md에 설계 근거 정리

### 의사결정과 그 근거

- **요금 정규화 — 세금 포함 총액(gross) 하나로 통일**
  Supplier B는 날짜별 요금을 아예 제공하지 않는다(부록 A.2). 두 공급사를 동시에 만족시키려면 둘 다 표현
  가능한 "숙박 전체 총액" 하나로 합칠 수밖에 없다고 판단했다. Supplier A는 날짜별 `nightlyRate+taxAmount`를
  합산해 동일한 형태로 환산한다.

- **재고 정규화 — 일자별 잔여 객실 수의 최소값**
  "요청 기간 전체를 예약할 수 있는 객실 수"가 최소 요구 조건이므로, 하루라도 재고가 0이면 전체 기간을
  예약할 수 없다는 논리로 최소값을 택했다.

- **중복 상품 병합은 하지 않음**
  Supplier A의 `A-10023`과 B의 `B77120`이 실제로는 같은 숙소일 수 있지만, 이를 판단할 공통 키가 스펙상
  존재하지 않는다. 이름 유사도 등으로 추정 병합하는 방법도 검토했지만, 이 과제의 필수 조건은 "같은 공급사
  상품이 항상 같은 내부 식별자로 조회되는 것"이지 "다른 공급사 상품을 하나로 묶는 것"이 아니라고 안내
  문서에 명시되어 있어, 필수 흐름에 시간을 쓰는 쪽을 택하고 병합은 선택 구현으로 남겨두었다.

- **매핑 동기화는 기동 시 1회**
  숙소 목록은 정적 콘텐츠 성격이라 매 검색 요청마다 조회할 이유가 없다. 주기적 스케줄러나 운영자 트리거
  API도 고려했지만, 관리자 기능은 비범위이고 이 과제의 "흐름이 끊김 없이 동작하는 것"이라는 우선순위에
  비추어 가장 단순한 방식을 택했다.

- **MVC + WebClient(부분 리액티브), WebFlux 전면 도입은 하지 않음**
  안내 문서가 WebFlux 전면 도입을 요구하지 않는다고 명시했다. 검색 핫패스(공급사 병렬 호출)만 `Mono`/`Flux`로
  처리하고, 나머지 계층(컨트롤러, 서비스 진입점)은 평소 익숙한 동기 MVC 스타일을 유지해 복잡도를 낮췄다.

### 막힌 지점

- Spring Boot 4.1.1에서 `spring-boot-starter-aop` 아티팩트가 더 이상 배포되지 않아 빌드가 실패했다.
  Maven Central 메타데이터를 직접 조회해 마지막 배포 버전이 `4.0.0-M2`에서 끊긴 것을 확인했고, 후속
  아티팩트인 `spring-boot-starter-aspectj`로 교체해 해결했다.

### 포기한 것과 이유

- 재시도 정책·서킷 브레이커(resilience4j)는 의존성만 추가해두고 구현하지 않았다. 타임아웃 + 부분 실패
  허용만으로 안내 문서가 요구하는 "연동 견고성 필수 네 가지"는 이미 충족되고, 남은 시간을 필수 흐름의
  실제 동작 검증에 우선 투입하는 것이 더 가치 있다고 판단했다.
- 요금/재고 캐시, 정규화 실패 데이터 격리, 통화(currency) 변환, 예약 대행 흐름은 모두 구현하지 않았다.
  검색 흐름 하나가 끊김 없이 동작하는 것을 최우선으로 두라는 안내에 따라, 이 과제의 핵심과 거리가 있는
  항목들은 설계 문서(README)에도 별도로 다루지 않고 범위에서 제외했다.

### 테스트

- Mock 서버(9090)와 본 애플리케이션(8080)을 실제로 동시에 기동해 아래 시나리오를 curl로 직접 검증했다.
  1. 정상 상태 — Supplier A/B 병렬 조회, 동일 숙소가 공급사별로 다른 내부 식별자를 갖는지, 요금/재고
     정규화 값이 올바른지 (수기로 A의 nightlyRate+taxAmount 합산 값과 대조)
  2. Supplier B 장애 모드(HTTP 200 + `resultCode: E503`) — 부분 실패 시 A의 결과만 반환되고
     `failedSuppliers`에 `SUPPLIER_B`가 노출되는지
  3. Supplier B 무응답 모드 — 응답 시간이 설정된 타임아웃(3초)과 일치하는 시점에 부분 실패로 처리되는지
- 위 세 시나리오 모두 기대한 대로 동작함을 확인했다.

### AI 활용 기록

이 프로젝트는 Claude(Anthropic의 AI 코딩 어시스턴트)를 이용해 개발했습니다.

- 과제 안내 문서를 Claude가 직접 읽고 요구사항(표준 모델 설계, 어댑터 경계 분리, 실패 판정 통일, 부분
  실패 허용, 병렬 조회, 매핑 저장)을 해석했습니다.
- 위에 정리한 설계 의사결정(요금/재고 정규화 규칙, 중복 상품 병합 여부, 매핑 동기화 시점, MVC+WebClient
  선택 등)은 Claude가 안내 문서의 제약 조건을 근거로 판단해 제시했고, 그 근거를 코드 주석과 README에
  남기도록 했습니다.
- 도메인 모델, 어댑터, Mock 서버, 검색 서비스 코드 작성과, 매 기능 완료 후 애플리케이션을 실제로 기동해
  curl로 동작(정상/장애/무응답 3가지 시나리오)을 검증하는 과정까지 Claude가 수행했습니다.
- 커밋·브랜치·PR·이슈 작성 컨벤션은 사전에 사람이 지정했고, 그 규칙을 따라 기능 단위로 세분화된 커밋과
  PR을 Claude가 생성했습니다.
- `spring-boot-starter-aop` 빌드 실패처럼 예상 밖의 문제가 나왔을 때도, Claude가 Maven Central을 직접
  조회해 원인을 특정하고 대체 아티팩트를 찾아 해결했습니다.

## Day 2 — Docker화, 요구사항 재점검, 버그 수정

### 수행 내용
- Docker 지원 추가 (멀티 스테이지 Dockerfile, docker-compose로 app + mock-supplier 2개 컨테이너)
- 안내 문서 문장을 다시 하나씩 짚어가며 요구사항 대비 구현 상태를 재점검
- 점검 중 발견한 버그 2건을 재현 테스트로 먼저 실패를 확인한 뒤 수정
- 이 과제의 핵심 조건(동일 공급사 상품 = 항상 동일 내부 식별자, 객실 타입은 숙소 내부에서만 유일)이
  서비스 레벨 유닛 테스트로만 검증되고 실제 DB 제약으로는 검증된 적이 없다는 것을 발견해
  `@DataJpaTest` 기반 테스트 추가
- 각주 지침(키워드 중심) 대비 실제 코드의 각주가 문장형으로 흐른 부분을 찾아 정리

### 발견하고 고친 문제

**1) 카탈로그 동기화가 공급사 간 격리되지 않음**
`CatalogSyncService.syncAll()`이 `catalogPorts.forEach()`로 순회해서, 한 공급사의
`fetchCatalog()`가 예외를 던지면 나머지 공급사 동기화까지 전부 멈췄다. 검색 흐름(④)에서는
공급사별로 실패를 격리해놓고 카탈로그 동기화에는 같은 원칙을 안 지킨 셈. 재현 테스트로 먼저
실패를 확인한 뒤(`CatalogSyncServiceIsolationTest`), 공급사별 try/catch로 격리해 고쳤다.

**2) 검색 인원(adults/children) 검증 누락**
`adults=0`, `adults=-1`, `children=-1` 같은 값이 검증 없이 그대로 공급사 API 호출에 전달되고
있었다. `adults>=1`, `children>=0` 검증을 추가하고 실제 서버를 기동해 경계값과 위반값 모두
curl로 확인했다.

**3) Docker 환경에서 H2 콘솔 접속 차단**
컨테이너 포트 포워딩을 거치면 H2가 요청 출처를 127.0.0.1로 인식하지 못해 `webAllowOthers`
기본값에 막혔다. 로컬(`./gradlew bootRun`)에서는 재현되지 않는, Docker 환경 특유의 문제.
`SPRING_H2_CONSOLE_SETTINGS_WEB_ALLOW_OTHERS=true`로 해결.

### 시행착오
- 처음엔 develop 브랜치를 로컬에서 여러 feature 브랜치와 `git merge`로 합쳐봤다가, 이게
  "리뷰 없는 병합"을 로컬에서 우회하는 셈이 될 수 있다고 판단해 즉시 `git reset --hard
  origin/develop`으로 되돌리고, 이미 순차적으로 쌓아둔 브랜치 위에서 이어 작업하는 방식으로
  바꿨다. PR 병합은 항상 명시적으로 요청받은 뒤에만 수행.
- `@DataJpaTest`가 Spring Boot 4.1.1에서 기존 패키지(`org.springframework.boot.test.
  autoconfigure.orm.jpa`)로는 컴파일이 안 돼서 처음엔 실패했다. jar 안을 직접 열어봐서 새 패키지
  (`org.springframework.boot.data.jpa.test.autoconfigure`)를 확인하고 고쳤다 — Spring Boot 4.x의
  모듈 재구성이 테스트 슬라이스 애너테이션 위치에도 영향을 준 사례.

### 테스트
- Docker: `docker compose build && up -d` 후 컨테이너 간 통신(app -> mock-supplier)으로
  카탈로그 동기화·검색·입력 검증까지 로컬 실행과 동일하게 동작하는 것을 확인
- 응답 필드 완전성: 안내 문서가 요구하는 7개 필드(내부 숙소/객실 식별자·명, 최대 수용 인원,
  예약 가능 객실 수, 출처 공급사, 요금, 부분 실패 사실)가 실제 응답에 전부 있는지 코드로 대조
- 비범위 항목(지역/정렬/페이징 파라미터)을 실수로 요구하거나 구현하지 않았는지 확인
- DB 유니크 제약을 `@DataJpaTest`로 직접 검증 (중복 저장 시 예외, 공급사가 다르면 각각 저장,
  재조회 시 동일 ID 반환) — 5건 추가, 전체 단위 테스트 15건으로 증가
